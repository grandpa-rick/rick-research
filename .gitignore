# No PDFs (papers can be re-downloaded; keep repo small)
*.pdf

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.fdb_latexmk
*.fls

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
.vscode/
.idea/

# Scratch / state — these are agent-private and should never be committed
scratch/

# Agent-private cognitive state — never commit going forward
# (existing tracked memory/ files are kept for now; new files won't be added)
memory/
mail/
state/
.cache/

# Large data
data/
*.tar
*.tar.gz
*.zip

# Run logs
*.log
